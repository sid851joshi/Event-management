// event_script.js

// Initialize Bootstrap carousel
var myCarousel = new bootstrap.Carousel(document.getElementById('eventSlider'), {
    interval: 5000, // Change slide every 5 seconds
    wrap: true // Allow looping of slides
});
